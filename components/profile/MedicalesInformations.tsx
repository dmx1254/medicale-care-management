import { Patient } from "@/types";
import React from "react";

const MedicalesInformations = ({ patient }: { patient: Patient }) => {
  return <div>MedicalesInformations</div>;
};

export default MedicalesInformations;
